.ammonite/
.bloop/
.bsp/
.idea/
.metals/
.vscode/
boot/
lib_managed/
metals.sbt
project/plugins/project/
project/target/
src_managed/
target/
log

# local data
local-data

# secrets
.env

# Local config — copy from the .example template, may contain throwaway local creds.
# Other env files (application-dev.conf, application-prod.conf) are committed
# because they only contain ${VAR} substitutions, no actual secrets.
modules/**/src/main/resources/application-local.conf

# AI
.claude/settings.local.json
CLAUDE.local.md